version: '3.8'

services:
  validator:
    build:
      context: .
      dockerfile: Dockerfile
    image: ha-blueprint-validator:latest
    container_name: blueprint-validator
    volumes:
      - .:/blueprints:ro
    working_dir: /blueprints
    command: ["*.yml", "*.yaml"]