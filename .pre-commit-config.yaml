repos:
  - repo: local
    hooks:
      - id: validate-yaml-blueprints
        name: Validate YAML Blueprints
        entry: ./validate.sh
        language: system
        files: \.(yml|yaml)$
        pass_filenames: true
        stages: [pre-commit]